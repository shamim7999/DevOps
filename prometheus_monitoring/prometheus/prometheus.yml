global:
  scrape_interval: 15s

rule_files:
  - alert_rules.yml

scrape_configs:

  - job_name: node_exporter

    static_configs:
      - targets:
          - x.x.x.x:9100
          - x.x.x.x:9100

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093